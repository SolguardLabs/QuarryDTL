{
  "name": "quarrydtl",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Go DTL liquidity extraction engine with TypeScript integration tests.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node scripts/build.mjs && node --test --experimental-strip-types \"tests/node/*.test.ts\"",
    "ci": "bash scripts/ci.sh",
    "loc": "node scripts/check-loc.mjs"
  },
  "engines": {
    "node": ">=24"
  },
  "devDependencies": {
    "prettier": "^3.8.3"
  }
}
